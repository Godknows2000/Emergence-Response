    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .hero-section {
            display: flex; /* Enables Flexbox */
            align-items: center; /* Centers content vertically */
            justify-content: center; /* Centers content horizontally */
            height: 75vh; /* Sets the section height to 100% of the viewport */
            padding: 50px 0; /* Optional padding */
            background: white; /* Background gradient */
            color: white;
            text-align: center; /* Centers text horizontally */
        }

        .hero-title {
            font-weight: bold;
            font-size: 2.5rem;
        background: linear-gradient(to right, #3fa7d6, #2C3D90);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .hero-subtitle {
            margin-top: 15px;
            color: #e0f5f2;
        }

        .col-md-6 img {
            height: 80%;
            width: 80%;
            object-fit: cover;
        }
    </style>
    <!-- Hero Section -->
    <div class="container-fluid hero-section">
        <div class="row align-items-center">
            <div class="col-md-6 text-start">
            <h1 class="hero-title">Swift, Reliable, Actionable Emergency Response Platform.</h1>
            <p class="text-body-secondary">
                    Act swiftly and effectively in times of crisis. Report emergencies, provide assistance,
                    and ensure safety with our intelligent AI-driven system designed to save lives.
            </p>
            <div class="cta-buttons">
                <a asp-area="EmergencyReports" asp-page="/Add" class="btn btn-success me-2">Report Emergence</a>
                <a asp-area="Identity" asp-page="/Account/Signup" class="btn btn-outline-primary"><i class="fas fa-user-plus"></i> Sign-up</a>
            </div>
            </div>
            <div class="col-md-6">
                <img src="~/images/emergence.png" alt="Emergence Response" class="img-fluid rounded">
            </div>
        </div>
    </div>

@section Styles {
    <link href="~/lib/leaflet/leaflet.css" rel="stylesheet" />
    <link href="~/lib/leaflet/Control.FullScreen.css" rel="stylesheet" />
    <style>
        .my-custom-scrollbar {
            position: relative;
            height: calc(100vh);
            overflow: auto;
        }

        .table-wrapper-scroll-y {
            display: block;
        }
    </style>
}

@section scripts {
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
}